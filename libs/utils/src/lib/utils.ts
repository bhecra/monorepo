export function utils(): string {
  return 'utils';
}

/**
 * Método para saludar al usuario (aqui se pone descripción gral)
 * @param name  nombre del usuario 
 * @returns  un saludo 
 */
export function greet(name: string): string{
  return `Hola ${name}`
}


export class Person {
  
}